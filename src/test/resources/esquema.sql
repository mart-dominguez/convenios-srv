CREATE SCHEMA CONVENIOS;

CREATE TABLE CONVENIOS.TIPO_FOO 
	(	ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, 
		DESCRIPCION VARCHAR(255) NOT NULL, 
		CONSTRAINT CONVENIOS.PK_SUB_FOO PRIMARY KEY (ID));
